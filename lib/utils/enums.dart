
enum CustomState {
  loading,
  done,
  error,
}

enum HttpMethod {
  post,
  get,
  put,
  delete
}

enum GameStatus {
  loading,
  playing,
  failed,
  success,
  greater,
  smaller
}

enum MessageType {
  dialog,
  toast,
}

enum DialogType {error, success, info, confirm, warning}